package com.people2000.user.web.action;

import org.apache.commons.lang.StringUtils;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import com.people2000.user.model.dto.UserAPIDTO;
import com.people2000.user.web.BaseAction;

/**
 * Created by xiaole on 2015/11/26.
 */
// @RequestMapping(value = "/userApi")
@Controller
public class UserAPIAction extends BaseAction {
	/*
	 * private UserAPIServiceClient userAPIServiceClient =
	 * UserAPIServiceClient.getInstanst();
	 * 
	 * @RequestMapping(value = "getUserInfo")
	 * 
	 * @ResponseBody public UserAPIDTO getUserInfo(UserAPIDTO userAPIDTO) {
	 * InputDTO<UserAPIDTO> inputDTO = new InputDTO<UserAPIDTO>(); String temp;
	 * { temp = userAPIDTO.getUsername(); if (StringUtils.isBlank(temp)) { temp
	 * = userAPIDTO.getMobile(); if (StringUtils.isBlank(temp)) { return null; }
	 * } } inputDTO.setData(userAPIDTO); OutputDTO<UserAPIDTO> outputDTO =
	 * userAPIServiceClient.getUserInfoByUserAPI(inputDTO); if
	 * ("0".equals(outputDTO.getCode())) { return outputDTO.getData(); } return
	 * null; }
	 */
}
